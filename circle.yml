version: 2.1
orbs:
  cypress: cypress-io/cypress@1
executors:
  with-chrome:
    docker:
      - image: 'cypress/browsers:node12.14.1-chrome83-ff77'
workflows:
  build:
    jobs:
      - cypress/run:
          executor: with-chrome
